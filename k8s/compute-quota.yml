apiVersion: v1
kind: ResourceQuota

metadata:
  name: compute-quota
  namespace: dev

spec:
  hard:
    pods: "10"
    #requests.cpu: "4"
    #requests.memory: 4Gi
    #limits.cpu: "16"
    #limits.memory: 16Gi
